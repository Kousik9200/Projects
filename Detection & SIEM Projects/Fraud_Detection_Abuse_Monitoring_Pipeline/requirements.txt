# No third-party dependencies — stdlib only (Python 3.10+)
# Optional enrichment / testing packages:
# requests>=2.31        # drop-in replacement for urllib if preferred
# pytest>=7.4
# pytest-cov>=4.1
